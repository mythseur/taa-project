database:
  override:
    - mysql -u ubuntu circle_test < my-database-setup.sql

test:
  override:
    - npm test
    - mvn integration-test
    - mvn -Pprod &
    - bootPid=$!
    - sleep 300s
    - gulp itest
    - kill bootPid
